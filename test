
-- [[ VOID SYSTEMS: FINAL BROADCAST ]]
-- 30 Second Managed Exit

local message = "sorry to everyone about the incidents yesterday and today. im speaking upon void, we have stopped."

local h = Instance.new("Hint")
h.Text = message
h.Parent = game.Workspace

-- Stay for exactly 30 seconds
task.wait(30)

-- Liquidate the message
h:Destroy()
